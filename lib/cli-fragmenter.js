// Generated by CoffeeScript 2.0.0-beta9-dev
var separator, sepExpr;
sepExpr = function (name) {
  return RegExp('#\\s\\(' + name + '\\)\\s*', 'i');
};
separator = function (code) {
  return code.match(sepExpr('ember'));
};
module.exports = function (code) {
  var emberStartIndex, firstMatch, fragments, matches, seperatorIndex;
  fragments = {
    js: '',
    ember: ''
  };
  matches = separator(code);
  if (!matches) {
    fragments.ember = code;
    return fragments;
  }
  firstMatch = matches[0];
  seperatorIndex = code.indexOf(firstMatch);
  emberStartIndex = seperatorIndex + firstMatch.length;
  fragments.js = code.slice(0, seperatorIndex);
  fragments.ember = code.slice(emberStartIndex);
  return fragments;
};
